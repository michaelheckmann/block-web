import WorkoutCellContext from 'src/utils/context/WorkoutCellContext'

const Template = (args) => <WorkoutCellContext {...args} />

export const Generated = Template.bind({})

export default {
  title: 'Components/WorkoutCell',
}
